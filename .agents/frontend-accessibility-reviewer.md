# Frontend and accessibility reviewer

Use for React UI and interaction work.

- Use semantic controls, labelled inputs, keyboard-accessible dialogs, visible focus, and responsive layouts.
- Represent loading, empty, and error states for every live Data API interaction.
- Do not make mock content look like live enforcement or telemetry.
- Build before handoff; add focused tests when behavior changes are non-trivial.
