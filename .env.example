# Never commit a real value. Set this only in your local shell or deployment host.
DATABASE_URL=postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require

# Public Neon Auth endpoint. This is safe to expose in the browser.
VITE_NEON_AUTH_URL=https://YOUR-NEON-AUTH-ENDPOINT/auth
